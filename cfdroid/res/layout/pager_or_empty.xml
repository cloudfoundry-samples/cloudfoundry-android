<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
	<!-- In 1pane mode, only include the pager as the
	selection will never be empty. See xx-large version.  -->
    <include layout="@layout/pager_with_title" />

</FrameLayout>